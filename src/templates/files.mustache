<!DOCTYPE HTML>
<html lang="en">
  <head>
    <title>Gherkins Detailer - Files List</title>
    {{> meta}}
  </head>
  <body>
    <header>
      <h1>Gherkins Detailer - Files List</h1>
    </header>

    {{> menu}}

    <main>
      {{#list}}
      <table>
        <thead>
          <tr>
            <th class="col-id">&num;</th>
            <th class="col-data">{{file}}</th>
            <th class="col-data">Similarities</th>
          </tr>
        </thead>
        <tbody>
          {{#files}}
          <tr class="{{class}}">
            <td class="col-id">{{id}}</td>
            <td class="col-data">{{text}}</td>
            <td class="col-data">
              {{#similarities}}
              <a href="./{{type}}.html#{{id}}" alt="{{text}}" title="{{text}}">{{id}}</a><br/>
              {{/similarities}}
            </td>
          </tr>
          {{/files}}
        </tbody>
      </table>
      {{/list}}
    </main>
    {{> footer}}
  </body>
</html>
