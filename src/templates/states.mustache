<!DOCTYPE HTML>
<html lang="en">
  <head>
    <title>Gherkins Detailer - States List</title>
    {{> meta}}
  </head>
  <body>
    <header>
      <h1>Gherkins Detailer - States List</h1>
    </header>

    {{> menu}}

    <main>
      {{#list}}
      <table>
        <thead>
          <tr>
            <th class="col-id">&num;</th>
            <th class="col-data">{{file}}</th>
            <th class="col-data">Similarities</th>
          </tr>
        </thead>
        <tbody>
          {{#states}}
          <tr class="{{class}}" id="{{id}}">
            <td class="col-id">{{id}}</td>
            <td class="col-data">{{text}}</td>
            <td class="col-data">
              {{#similarities}}
              <a href="./{{type}}.html#{{id}}" alt="{{text}}" title="{{text}}">{{text}} ({{id}})</a><br/>
              {{/similarities}}
            </td>
          </tr>
          {{/states}}
        </tbody>
      </table>
      {{/list}}
    </main>
    {{> footer}}
  </body>
</html>
